from typing import Optional
from ..base_plugin import BasePlugin

class PsList(BasePlugin):
    """Lists the processes present in a particular linux memory image"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pslist.PsList"])

class PsTree(BasePlugin):
    """Plugin for listing processes in a tree based on their parent process ID"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pstree.PsTree"])

class PsScan(BasePlugin):
    """Scans for processes present in a particular linux image"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.psscan.PsScan"])

class PsAux(BasePlugin):
    """Lists processes with their command line arguments"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.psaux.PsAux"])

class PsCallStack(BasePlugin):
    """Enumerates the call stack of each task"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pscallstack.PsCallStack"])

class PIDHashTable(BasePlugin):
    """Enumerates processes through the PID hash table"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pidhashtable.PIDHashTable"])

class Bash(BasePlugin):
    """Recovers bash command history from memory"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.bash.Bash"])

class Boottime(BasePlugin):
    """Shows the time the system was started"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.boottime.Boottime"])

class Capabilities(BasePlugin):
    """Lists process capabilities"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.capabilities.Capabilities"])

class Check_afinfo(BasePlugin):
    """Verifies the operation function pointers of network protocols"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.check_afinfo.Check_afinfo"])

class Check_creds(BasePlugin):
    """Checks if any processes are sharing credential structures"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.check_creds.Check_creds"])

class Check_idt(BasePlugin):
    """Checks if the IDT has been altered"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.check_idt.Check_idt"])

class Check_modules(BasePlugin):
    """Compares module list to sysfs info, if available"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.check_modules.Check_modules"])

class Check_syscall(BasePlugin):
    """Check system call table for hooks"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.check_syscall.Check_syscall"])

class EBPF(BasePlugin):
    """Enumerate eBPF programs"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.ebpf.EBPF"])

class Elfs(BasePlugin):
    """Lists all memory mapped ELF files for all processes"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.elfs.Elfs"])

class Envars(BasePlugin):
    """Lists processes with their environment variables"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.envars.Envars"])

class Fbdev(BasePlugin):
    """Extract framebuffers from the fbdev graphics subsystem"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.graphics.fbdev.Fbdev"])

class Hidden_modules(BasePlugin):
    """Carves memory to find hidden kernel modules"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.hidden_modules.Hidden_modules"])

class IOMem(BasePlugin):
    """Generates an output similar to /proc/iomem on a running system"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.iomem.IOMem"])

class IpAddr(BasePlugin):
    """Lists network interface information for all devices"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.ip.Addr"])

class IpLink(BasePlugin):
    """Lists information about network interfaces similar to `ip link show`"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.ip.Link"])

class Kallsyms(BasePlugin):
    """Kallsyms symbols enumeration plugin"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.kallsyms.Kallsyms"])

class Keyboard_notifiers(BasePlugin):
    """Parses the keyboard notifier call chain"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.keyboard_notifiers.Keyboard_notifiers"])

class Kmsg(BasePlugin):
    """Kernel log buffer reader"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.kmsg.Kmsg"])

class Kthreads(BasePlugin):
    """Enumerates kthread functions"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.kthreads.Kthreads"])

class LibraryList(BasePlugin):
    """Enumerate libraries loaded into processes"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.library_list.LibraryList"])

class Lsmod(BasePlugin):
    """Lists loaded kernel modules"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.lsmod.Lsmod"])

class Lsof(BasePlugin):
    """Lists open files for each processes"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.lsof.Lsof"])

class Malfind(BasePlugin):
    """Lists process memory ranges that potentially contain injected code"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.malfind.Malfind"])

class ModuleExtract(BasePlugin):
    """Recreates an ELF file from a specific address in the kernel"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.module_extract.ModuleExtract"])

class Modxview(BasePlugin):
    """Centralize lsmod, check_modules and hidden_modules results to efficiently spot modules presence and taints"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.modxview.Modxview"])

class MountInfo(BasePlugin):
    """Lists mount points on processes mount namespaces"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.mountinfo.MountInfo"])

class Netfilter(BasePlugin):
    """Lists Netfilter hooks"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.netfilter.Netfilter"])

class Files(BasePlugin):
    """Lists files from memory"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pagecache.Files"])

class InodePages(BasePlugin):
    """Lists and recovers cached inode pages"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pagecache.InodePages"])

class RecoverFs(BasePlugin):
    """Recovers the cached filesystem (directories, files, symlinks) into a compressed tarball"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.pagecache.RecoverFs"])

class ProcMaps(BasePlugin):
    """Lists all memory maps for all processes"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.proc.Maps"])

class Ptrace(BasePlugin):
    """Enumerates ptrace's tracer and tracee tasks"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.ptrace.Ptrace"])

class Sockstat(BasePlugin):
    """Lists all network connections for all processes"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.sockstat.Sockstat"])

class CheckFtrace(BasePlugin):
    """Detect ftrace hooking"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.tracing.ftrace.CheckFtrace"])

class PerfEvents(BasePlugin):
    """Lists performance events for each process"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.tracing.perf_events.PerfEvents"])

class CheckTracepoints(BasePlugin):
    """Detect tracepoints hooking"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.tracing.tracepoints.CheckTracepoints"])

class TtyCheck(BasePlugin):
    """Checks tty devices for hooks"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.tty_check.tty_check"])

class VmaRegExScan(BasePlugin):
    """Scans all virtual memory areas for tasks using RegEx"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.vmaregexscan.VmaRegExScan"])

class VmaYaraScan(BasePlugin):
    """Scans all virtual memory areas for tasks using yara"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.vmayarascan.VmaYaraScan"])

class VMCoreInfo(BasePlugin):
    """Enumerate VMCoreInfo tables"""
    
    async def run(self, memory_dump_path: str) -> str:
        memory_dump_path = self.validate_memory_dump(memory_dump_path)
        if memory_dump_path.startswith("Error"):
            return memory_dump_path
            
        return await self.volatility_runner(["-f", memory_dump_path, "linux.vmcoreinfo.VMCoreInfo"]) 